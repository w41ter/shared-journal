[package]
name = "shared-journal-proto"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
shared-journal-common = { version = "0.1", path = "../common" }

prost = "0.9"
tonic = "0.6"

[build-dependencies]
tonic-build = "0.6"
